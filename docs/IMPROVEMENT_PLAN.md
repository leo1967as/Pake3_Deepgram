# 📋 Pake3_Deepgram - Improvement Plan
> **เอกสารสังเคราะห์จาก Feedback และแผนการปรับปรุง**  
> **จัดทำวันที่:** 2 กุมภาพันธ์ 2026  
> **สถานะปัจจุบัน:** Functional Prototype (65% Production Ready)

---

## 1. 📊 Feedback Synthesis (สรุปประเด็นปัญหา)

### 1.1 หมวด Technical (เทคนิค)

| ลำดับ | ประเด็น | ความสำคัญ | รายละเอียด |
|:-----:|---------|:----------:|-------------|
| T1 | **ขาด Retry Logic สำหรับ WebSocket** | 🔴 Critical | หากอินเทอร์เน็ตสะดุด 2 วินาที Pipeline ทั้งหมดจะพัง |
| T2 | **Local Socket Blocking** | 🔴 Critical | Port 8765 - หาก GUI crash จะทำให้ลำดับการประมวลผลเพี้ยน |
| T3 | **Hard-coded Path** | 🟠 High | `.venv310/Scripts/python.exe` ทำให้ Deploy ในเครื่องอื่นยาก |
| T4 | **ขาด Buffer Queue** | 🟠 High | ไม่มีระบบเก็บข้อมูลชั่วคราวขณะ API/GUI ขัดข้อง |
| T5 | **ขาด Error Handling ใน GUI** | 🟠 High | JSON Parse ผิด Format → GUI ปิดตัวทันที |
| T6 | **ขาด Fallback AI** | 🟡 Medium | หาก Gemini ช้า ไม่มี AI สำรองทำงานแทน |
| T7 | **ขาด Logging System** | 🟡 Medium | Debug ปัญหาหน้างานทำได้ยาก |
| T8 | **Scalability จำกัด** | 🟡 Medium | รองรับแค่ 1 Video Stream ต่อ Instance |
| T9 | **โครงสร้างโค้ด Monolithic** | 🟠 High | ไฟล์ใหญ่ 800+ บรรทัด ทดสอบ/บำรุงรักษายาก |

### 1.2 หมวด Security (ความปลอดภัย)

| ลำดับ | ประเด็น | ความสำคัญ | รายละเอียด |
|:-----:|---------|:----------:|-------------|
| S1 | **Source Code ไม่มีการป้องกัน** | 🔴 Critical | Python Plain Text - ลูกค้าสามารถก๊อปปี้ไปสร้างผลิตภัณฑ์แข่งได้ |
| S2 | **ไม่มี License Key System** | 🟠 High | ไม่สามารถควบคุมการใช้งานหรือผูกกับ Hardware ได้ |

### 1.3 หมวด Business/Product (ธุรกิจ)

| ลำดับ | ประเด็น | ความสำคัญ | รายละเอียด |
|:-----:|---------|:----------:|-------------|
| B1 | **"ทรงตัว 100%" - วิเคราะห์ไม่มีคุณค่า** | 🔴 Critical | AI ตอบ "ทรงตัว" ตลอด ไม่มีการแยกแยะ Hawkish/Dovish |
| B2 | **ไม่มี Market Gap ในไทย** | 🟠 High | กองทุน/โบรกเกอร์มีเครื่องมือดีกว่าอยู่แล้ว |
| B3 | **Business Model ไม่ชัด** | 🟠 High | ขาย "ระบบ" vs ขาย "บริการ" ยังไม่ตัดสินใจ |
| B4 | **Prompt Injection / Hallucination** | 🟡 Medium | AI อาจตีความผิดหากเสียงรบกวนมาก |

### 1.4 หมวด DevOps/Deployment

| ลำดับ | ประเด็น | ความสำคัญ | รายละเอียด |
|:-----:|---------|:----------:|-------------|
| D1 | **ไม่มี Docker Container** | 🟠 High | Environment ไม่คงที่ ติดตั้งในเครื่องอื่นยาก |
| D2 | **พึ่งพา External API 3 ตัว** | 🟡 Medium | Deepgram, OpenRouter, n8n - หากตัวใดล่มระบบหยุด |

---

## 2. 🎯 Improvement Action Plan (แผนการปรับปรุง)

### 🔴 Critical Priority (ต้องแก้ทันที)

| # | Task | Why | Expected Outcome |
|:-:|------|-----|------------------|
| 1 | **แก้ไข AI Analysis Logic** | ระบบตอบ "ทรงตัว 100%" ทำให้ไม่มีมูลค่าต่อผู้ใช้ | AI สามารถระบุ Hawkish/Dovish ได้อย่างน้อย 70% ของ Speech |
| 2 | **เพิ่ม WebSocket Retry Logic** | Internet สะดุด 2 วิ = Pipeline พัง | ระบบ Auto-reconnect ภายใน 5 วินาที โดยไม่สูญเสียข้อมูล |
| 3 | **แก้ไข GUI Socket Handling** | GUI crash ทำให้ระบบค้าง | GUI crash ไม่กระทบ Transcription pipeline |
| 4 | **เพิ่ม Error Handling (JSON Parse)** | AI ตอบผิด Format = GUI ปิดตัว | แสดง Error message แทนการ crash |

### 🟠 High Priority (แก้ภายใน 2 สัปดาห์)

| # | Task | Why | Expected Outcome |
|:-:|------|-----|------------------|
| 5 | **เพิ่ม Buffer Queue System** | ป้องกัน Data loss ขณะ API ขัดข้อง | เก็บ Transcript ได้ 100 รายการในขณะ Offline |
| 6 | **สร้าง Dockerfile** | Environment ต่างกัน = ระบบไม่ทำงาน | Deploy ได้ทุกเครื่องด้วย `docker-compose up` |
| 7 | **ลบ Hard-coded Path** | Deploy ในเครื่องอื่นไม่ได้ | ใช้ Environment Variable หรือ Relative Path |
| 8 | **สร้าง License Key System** | ป้องกันการ Copy โค้ดไปใช้ | ระบบตรวจสอบ License ก่อนเริ่มทำงาน |
| 9 | **Compile Core Logic ด้วย Cython** | ป้องกัน Source Code ถูกขโมย | ไฟล์หลักเป็น .pyd/.so ที่ Decompile ยาก |
| 10 | **ปรับ Business Model → บริการวิเคราะห์** | ไม่มีตลาดสำหรับ "ขายระบบ" | มี Telegram/Discord Channel พร้อมรับลูกค้า |
| 11 | **Refactor เป็น Modular Architecture** | โค้ด 800+ บรรทัดในไฟล์เดียว ทดสอบ/บำรุงรักษายาก | โครงสร้างแยกเป็น 6 โมดูลย่อย |

### 🟡 Medium Priority (แก้ภายใน 1 เดือน)

| # | Task | Why | Expected Outcome |
|:-:|------|-----|------------------|
| 12 | **เพิ่ม Fallback AI** | Gemini ช้า/ล่ม = หยุดทำงาน | สลับไป Groq/Llama-3 อัตโนมัติ |
| 13 | **สร้าง Logging System** | Debug ปัญหาหน้างานทำได้ยาก | Log file แยกตามวัน + Error alerts |
| 14 | **เพิ่ม Prompt Guard** | Hallucination/Prompt Injection | มี Validation layer ก่อนส่งผลลัพธ์ |
| 15 | **รองรับ Multi-Stream** | Scalability จำกัด | รองรับ 3+ streams พร้อมกัน |

---

## 3. 🗺️ Project Roadmap

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         PAKE3_DEEPGRAM ROADMAP 2026                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  PHASE 1: IMMEDIATE FIXES          PHASE 2: ENHANCEMENTS                   │
│  (สัปดาห์ที่ 1-2)                    (สัปดาห์ที่ 3-4)                         │
│  ━━━━━━━━━━━━━━━━━━━━              ━━━━━━━━━━━━━━━━━━━━                     │
│  ■ แก้ไข AI Logic                 ■ สร้าง Docker Container                 │
│  ■ WebSocket Retry                ■ License Key System                     │
│  ■ GUI Error Handling             ■ Cython Compilation                     │
│  ■ Buffer Queue System            ■ Telegram Bot Integration               │
│  ■ Refactor Modular (Phase 1)     ■ Refactor Modular (Phase 2)             │
│                                   ■ Business Model Launch                  │
│                                                                             │
│  PHASE 3: SCALING & FUTURE                                                 │
│  (เดือนที่ 2+)                                                              │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━                                                 │
│  ■ Multi-Stream Support                                                    │
│  ■ Fallback AI System                                                      │
│  ■ Advanced Logging & Monitoring                                           │
│  ■ Web Dashboard (Optional)                                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Phase 1: Immediate Fixes (0-2 สัปดาห์)

| สัปดาห์ | วัน | งาน | Deliverable |
|:-------:|:---:|-----|-------------|
| 1 | 1-2 | **แก้ไข AI Analysis Prompt** | Prompt ใหม่ที่มีกฎ Hawkish/Dovish ชัดเจน |
| 1 | 3-4 | **เพิ่ม WebSocket Retry Logic** | `pake_live.py` ที่ reconnect อัตโนมัติ |
| 1 | 5-7 | **แก้ไข GUI Error Handling** | `pake_gui.py` ที่ไม่ crash เมื่อ JSON ผิด |
| 2 | 1-3 | **เพิ่ม Buffer Queue** | Queue system ด้วย Python `collections.deque` |
| 2 | 4-5 | **ลบ Hard-coded Path** | ใช้ `pathlib` และ Environment Variables |
| 2 | 4-5 | **Refactor Phase 1: แยก Utils** | `config.py`, `logger.py`, `styles.py` |
| 2 | 6-7 | **Testing & Bug Fixes** | ทดสอบกับ Live stream จริง 3 ครั้ง |

### Phase 2: Enhancements (2-4 สัปดาห์)

| สัปดาห์ | วัน | งาน | Deliverable |
|:-------:|:---:|-----|-------------|
| 3 | 1-2 | **Refactor Phase 2: แยก Core** | `analyzer.py`, `translator.py`, `sentiment.py` |
| 3 | 3-4 | **Refactor Phase 3: แยก Network** | `websocket_client.py`, `socket_server.py`, `api_client.py` |
| 3 | 5-7 | **สร้าง Dockerfile + docker-compose** | Container พร้อม Deploy |
| 4 | 1-2 | **สร้าง License Key System** | Hardware ID verification |
| 4 | 2-3 | **Compile ด้วย Cython** | `.pyd` files สำหรับ `core/` |
| 4 | 3-4 | **สร้าง Telegram Bot** | Bot ส่งสัญญาณ Hawkish/Dovish |
| 4 | 5-6 | **เตรียม Sales Material** | Landing page + Pricing |
| 4 | 7 | **Soft Launch** | หาลูกค้า 3 รายแรก |

### Phase 3: Scaling & Future (1 เดือน+)

| เดือน | งาน | Deliverable |
|:-----:|-----|-------------|
| 2 | **Multi-Stream Architecture** | รองรับ 3+ streams พร้อมกัน |
| 2 | **Fallback AI Integration** | Groq/Llama-3 เป็น Backup |
| 2 | **Advanced Logging** | Centralized logging + Alerts |
| 3+ | **Web Dashboard** | UI สำหรับดู Analytics |
| 3+ | **Mobile App** | iOS/Android notification |

---

## 4. ⚠️ Risk Mitigation Update

### ความเสี่ยงและแผนรับมือ

| Risk ID | ความเสี่ยง | ความรุนแรง | โอกาสเกิด | แผนรับมือ |
|:-------:|-----------|:----------:|:---------:|----------|
| R1 | **Deepgram API ล่ม/เปลี่ยน** | สูง | ต่ำ | เตรียม Assembly AI หรือ Whisper Local เป็น Fallback |
| R2 | **OpenRouter/Gemini ช้า** | สูง | กลาง | เพิ่ม Groq (Llama-3) เป็น Backup + Timeout 5 วินาที |
| R3 | **Internet ขาดหาย** | สูง | กลาง | Buffer Queue + Auto-reconnect ภายใน 5 วินาที |
| R4 | **GUI Crash** | กลาง | กลาง | แยก Process + IPC ผ่าน Message Queue |
| R5 | **ลูกค้า Copy โค้ด** | สูง | สูง | Cython + License Key + ขายเป็น Service ไม่ใช่ Source |
| R6 | **AI Hallucination** | กลาง | กลาง | Validation layer + Confidence score threshold |
| R7 | **ไม่มีลูกค้า** | สูง | กลาง | Pivot เป็น "บริการวิเคราะห์" + ทำการตลาดผ่าน Social Media |

### Contingency Plans

```
┌────────────────────────────────────────────────────────────────┐
│                    CONTINGENCY DECISION TREE                   │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  [Deepgram ล่ม?]                                               │
│       │                                                        │
│       ├── Yes → สลับไป Assembly AI                             │
│       │          └── Assembly AI ล่ม? → ใช้ Whisper Local      │
│       └── No → ทำงานปกติ                                       │
│                                                                │
│  [AI ตอบช้า > 5 วินาที?]                                       │
│       │                                                        │
│       ├── Yes → สลับไป Groq (Llama-3)                          │
│       │          └── แจ้ง User ว่าใช้ Backup AI               │
│       └── No → ทำงานปกติ                                       │
│                                                                │
│  [Internet ขาด?]                                               │
│       │                                                        │
│       ├── Yes → เก็บ Transcript ใน Buffer Queue                │
│       │          └── Reconnect ทุก 5 วินาที (max 10 ครั้ง)     │
│       │          └── หลัง Reconnect → ส่ง Backlog ทั้งหมด      │
│       └── No → ทำงานปกติ                                       │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 5. 📈 Success Metrics (ตัวชี้วัดความสำเร็จ)

### 5.1 Technical Metrics (ตัวชี้วัดทางเทคนิค)

| Metric | เป้าหมาย | วิธีวัด |
|--------|:--------:|--------|
| **System Uptime** | ≥ 99% | Log หมดเวลา / เวลาทั้งหมด |
| **WebSocket Reconnect Time** | ≤ 5 วินาที | Timestamp จาก disconnect → reconnect |
| **GUI Crash Rate** | 0 ครั้ง/สัปดาห์ | Error log monitoring |
| **AI Response Latency** | ≤ 3 วินาที | Timestamp จาก transcript → analysis |
| **Data Loss Rate** | 0% | เปรียบเทียบ transcript count กับ video timeline |

### 5.2 Business Metrics (ตัวชี้วัดทางธุรกิจ)

| Metric | Phase 1 | Phase 2 | Phase 3 |
|--------|:-------:|:-------:|:-------:|
| **Analysis Accuracy** (Non-neutral %) | ≥ 50% | ≥ 70% | ≥ 80% |
| **Paying Customers** | 0 | 3 ราย | 10 ราย |
| **Monthly Revenue** | $0 | $600 | $2,000 |
| **Customer Retention** | N/A | N/A | ≥ 80% |

### 5.3 Quality Gates (ด่านตรวจคุณภาพ)

ระบบจะถือว่า **"พร้อมสู่ตลาด 100%"** เมื่อผ่านเงื่อนไขทั้งหมดนี้:

| # | Quality Gate | เกณฑ์ผ่าน | สถานะปัจจุบัน |
|:-:|--------------|----------|:-------------:|
| 1 | **AI Analysis ไม่ใช่ "ทรงตัว" ตลอด** | Non-neutral ≥ 70% | ❌ ไม่ผ่าน |
| 2 | **ระบบรัน 4 ชม. ไม่ crash** | 0 crashes | ❌ ยังไม่ทดสอบ |
| 3 | **Reconnect อัตโนมัติเมื่อ Internet หลุด** | ≤ 5 วินาที | ❌ ไม่ผ่าน |
| 5 | **ระบบแยกเป็น Modular Architecture** | 6 โมดูลแยกชัดเจน | ❌ ไม่ผ่าน |
| 6 | **มี Docker Container พร้อม Deploy** | `docker-compose up` สำเร็จ | ❌ ไม่มี |
| 7 | **มี License Protection** | Cython + Key | ❌ ไม่มี |
| 8 | **มีช่องทางส่งสัญญาณ (Telegram/Discord)** | Bot ทำงานได้ | ❌ ไม่มี |
| 9 | **มีลูกค้าจ่ายเงินจริง** | ≥ 1 ราย | ❌ ยังไม่มี |

### 5.4 Definition of Done (นิยาม "เสร็จสิ้น")

```
┌─────────────────────────────────────────────────────────────┐
│              🎯 PRODUCTION READY CHECKLIST                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Technical Readiness:                                       │
│  [ ] WebSocket auto-reconnect ทำงานได้                      │
│  [ ] Buffer Queue เก็บข้อมูลได้ 100+ รายการ                 │
│  [ ] GUI ไม่ crash เมื่อ AI ตอบผิด format                   │
│  [ ] Logging system บันทึก Error ได้                        │
│  [ ] Docker container build สำเร็จ                          │
│  [ ] Modular architecture แยกเป็น 6 โมดูล                   │
│                                                             │
│  Security Readiness:                                        │
│  [ ] Core files compiled ด้วย Cython                        │
│  [ ] License key verification ทำงานได้                      │
│  [ ] API keys ไม่ hard-coded ในโค้ด                         │
│  [ ] core/ module แยกออกพร้อม Compile                       │
│                                                             │
│  Business Readiness:                                        │
│  [ ] AI วิเคราะห์ได้ Hawkish/Dovish อย่างน้อย 70%           │
│  [ ] Telegram/Discord bot ส่งสัญญาณได้                      │
│  [ ] มี Pricing structure ชัดเจน                            │
│  [ ] มีลูกค้าทดลองใช้อย่างน้อย 3 ราย                        │
│                                                             │
│  Documentation:                                             │
│  [ ] README.md สำหรับติดตั้ง                                │
│  [ ] User guide สำหรับลูกค้า                                │
│  [ ] Troubleshooting guide                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 🏗️ Modular Architecture Plan (แผนการแยกโมดูล)

### 6.1 โครงสร้างปัจจุบัน (ปัญหา)

```
src/
├── pake_gui.py       # 821 บรรทัด - Socket + Translation + Analysis + GUI + Styles
├── pake_live.py      # 422 บรรทัด - WebSocket + Batch + Broadcast + Webhook
└── pake_deepgram.py  # 214 บรรทัด - Download + Transcribe + N8N
```

| ปัญหา | ผลกระทบ |
|-------|---------|
| ไฟล์ใหญ่เกินไป (800+ บรรทัด) | หา Bug ยาก, แก้ไขเสี่ยงพัง |
| Logic รวมกันหมด | Unit Test ไม่ได้, Reuse ยาก |
| ไม่สามารถ Compile บางส่วน | ป้องกัน IP ไม่ได้ (ต้อง Compile ทั้งหมด) |
| Dependencies ผูกกัน | เปลี่ยน 1 จุดกระทบทั้งระบบ |

### 6.2 โครงสร้างใหม่ (เป้าหมาย)

```
src/
├── __init__.py
├── main.py                     # Entry point - รัน GUI หรือ CLI
│
├── core/                       # 🔒 Business Logic (Compile ด้วย Cython)
│   ├── __init__.py
│   ├── analyzer.py             # AI Analysis logic + Prompts
│   ├── translator.py           # Translation logic  
│   ├── sentiment.py            # Hawkish/Dovish rules + Keywords
│   └── prompts.py              # Prompt templates
│
├── network/                    # Network Layer
│   ├── __init__.py
│   ├── websocket_client.py     # Deepgram WebSocket + Retry Logic
│   ├── socket_server.py        # GUI TCP Server
│   └── api_client.py           # OpenRouter/Webhook HTTP Client
│
├── gui/                        # UI Layer  
│   ├── __init__.py
│   ├── main_window.py          # Main GUI Window
│   ├── styles.py               # Qt Styles (DARK_STYLE)
│   └── workers.py              # QThread Workers (Translate, Analysis)
│
├── utils/                      # Utilities
│   ├── __init__.py
│   ├── config.py               # Environment Variables + Constants
│   ├── logger.py               # Logging System
│   └── buffer.py               # Buffer Queue System
│
└── models/                     # Data Models
    ├── __init__.py
    └── schemas.py              # Segment, Batch, Session dataclasses
```

### 6.3 แผนการ Refactor (3 Phases)

#### Phase 1: แยก Utils (สัปดาห์ที่ 2) - Low Risk ✅

| ไฟล์ใหม่ | ย้ายมาจาก | หน้าที่ |
|---------|----------|--------|
| `utils/config.py` | ทุกไฟล์ | รวม Environment Variables, Constants |
| `utils/logger.py` | ใหม่ | Logging system + File rotation |
| `gui/styles.py` | `pake_gui.py` | Qt Stylesheet (DARK_STYLE) |

**ประโยชน์ทันที:**
- Config อยู่ที่เดียว ไม่กระจาย
- Debug ได้ด้วย Log file
- แก้ Style ไม่ต้องแตะ Logic

#### Phase 2: แยก Core (สัปดาห์ที่ 3) - Medium Risk ⚠️

| ไฟล์ใหม่ | ย้ายมาจาก | หน้าที่ |
|---------|----------|--------|
| `core/analyzer.py` | `pake_gui.py` (AnalysisWorker) | AI Analysis logic |
| `core/translator.py` | `pake_gui.py` (TranslateWorker) | Translation logic |
| `core/sentiment.py` | `pake_gui.py` (Prompts) | Hawkish/Dovish rules |
| `core/prompts.py` | `pake_gui.py` | Prompt templates |

**ประโยชน์:**
- Compile เฉพาะ `core/` ด้วย Cython ได้
- Unit Test AI Logic แยกจาก GUI
- เปลี่ยน Prompt ไม่ต้องแตะ GUI

#### Phase 3: แยก Network (สัปดาห์ที่ 3-4) - Medium Risk ⚠️

| ไฟล์ใหม่ | ย้ายมาจาก | หน้าที่ |
|---------|----------|--------|
| `network/websocket_client.py` | `pake_live.py` | Deepgram connection + Retry |
| `network/socket_server.py` | `pake_gui.py` (SocketServerThread) | GUI TCP Server |
| `network/api_client.py` | ทั้งสองไฟล์ | httpx wrapper + Error handling |

**ประโยชน์:**
- Retry logic อยู่ที่เดียว
- Mock API ได้ใน Testing
- เปลี่ยน Provider ไม่กระทบ Logic

### 6.4 Migration Strategy (ไม่พัง Production)

```
┌─────────────────────────────────────────────────────────────────┐
│              SAFE MIGRATION STRATEGY                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 1: สร้าง Module ใหม่ (ไม่ลบของเก่า)                       │
│          └── สร้าง utils/config.py แต่ยังไม่ Import             │
│                                                                 │
│  Step 2: ทดสอบ Module ใหม่แยก                                   │
│          └── pytest tests/test_config.py                       │
│                                                                 │
│  Step 3: Import ทีละไฟล์ (เริ่มจากไฟล์เล็ก)                     │
│          └── pake_deepgram.py → from utils.config import ...   │
│                                                                 │
│  Step 4: ทดสอบ Integration                                      │
│          └── รัน Live test 1 ครั้ง                              │
│                                                                 │
│  Step 5: ย้ายไฟล์ถัดไป                                          │
│          └── pake_live.py → from utils.config import ...       │
│                                                                 │
│  Step 6: ลบ Code เก่าหลังทดสอบสำเร็จ                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.5 ตัวอย่างโค้ดหลัง Refactor

**ก่อน (pake_gui.py - 821 บรรทัด):**
```python
# ทุกอย่างอยู่ในไฟล์เดียว
import os
from dotenv import load_dotenv
load_dotenv()
OPENROUTER_API_KEY = os.getenv("OPENROUTER_KEY", "")

DARK_STYLE = """..."""  # 30 บรรทัด

class SocketServerThread(QThread): ...  # 50 บรรทัด
class TranslateWorker(QObject): ...     # 80 บรรทัด  
class AnalysisWorker(QObject): ...      # 150 บรรทัด
class MainWindow(QMainWindow): ...      # 400 บรรทัด
```

**หลัง (แยกเป็นโมดูล):**
```python
# main.py - 20 บรรทัด
from gui.main_window import MainWindow
from utils.config import Config
from utils.logger import setup_logging

def main():
    setup_logging()
    config = Config()
    app = QApplication(sys.argv)
    window = MainWindow(config)
    window.show()
    sys.exit(app.exec())
```

```python
# gui/main_window.py - 200 บรรทัด (ลดลง 50%)
from gui.styles import DARK_STYLE
from gui.workers import TranslateWorker, AnalysisWorker
from network.socket_server import SocketServerThread

class MainWindow(QMainWindow):
    # เฉพาะ UI Logic เท่านั้น
    ...
```

```python
# core/analyzer.py - 100 บรรทัด (แยกออกมา)
from core.sentiment import SentimentRules
from core.prompts import ANALYSIS_PROMPT

class Analyzer:
    def analyze(self, text: str, context: str) -> dict:
        # Business Logic อยู่ที่นี่
        # สามารถ Compile ด้วย Cython ได้
        ...
```

---

## 7. 📝 Action Summary

### สิ่งที่ต้องทำทันที (This Week)

1. **[CRITICAL]** แก้ไข AI Analysis Prompt ใน `pake_gui.py` ให้มีกฎ Hawkish/Dovish ชัดเจน
2. **[CRITICAL]** เพิ่ม WebSocket Retry Logic ใน `pake_live.py`
3. **[CRITICAL]** เพิ่ม try-except สำหรับ JSON parsing ใน GUI

### การตัดสินใจที่ต้องทำ

| คำถาม | ตัวเลือก | คำแนะนำ |
|-------|---------|---------|
| Business Model? | ขายระบบ vs ขายบริการ | **ขายบริการ** (Subscription) |
| Target Market? | กองทุน vs รายย่อย | **รายย่อยระดับสูง** (พอร์ต >50 ล้าน) |
| Deployment? | Docker vs Native | **Docker** (ง่ายต่อการติดตั้ง) |
| IP Protection? | None vs Cython | **Cython + License Key** |

---

> **หมายเหตุ:** เอกสารนี้ควรได้รับการ Review และ Update ทุกสัปดาห์  
> **ผู้รับผิดชอบ:** Lead Developer  
> **Next Review Date:** 9 กุมภาพันธ์ 2026
